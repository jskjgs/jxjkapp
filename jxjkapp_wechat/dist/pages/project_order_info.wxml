
  <view id="page-index-order-info">
    
  <view class="comp-order-info">
    
  <view class="comp-picker-list">
    <view class="menu--default">
      <block wx:for="{{ $OrderInfo$PickerList$ITEMS }}" wx:for-index="$OrderInfo$PickerList$index" wx:for-item="$OrderInfo$PickerList$item" wx:key="$OrderInfo$PickerList$index">
        <view class="menu--default__item">
          <text class="item-label">{{ $OrderInfo$PickerList$item.label }}</text>
          <picker  wx:if="{{ !$OrderInfo$PickerList$readonly }}" value="{{ $OrderInfo$PickerList$pickedIndex[$OrderInfo$PickerList$item.picker.valueBind] === -1 ? 0 : $OrderInfo$PickerList$pickedIndex[$OrderInfo$PickerList$item.picker.valueBind] }}" range="{{ $OrderInfo$PickerList$lists[$OrderInfo$PickerList$item.picker.rangeBind] }}" range-key="label" bindchange="$OrderInfo$PickerList$handlePickerChange" data-wpyhandlepickerchange-a="{{ $OrderInfo$PickerList$item.picker.valueBind }}" data-wpyhandlepickerchange-b="{{ $OrderInfo$PickerList$item.picker.rangeBind }}">
            <view class="item-value">
              <text>{{ $OrderInfo$PickerList$pickeds[$OrderInfo$PickerList$item.picker.valueBind] !== $OrderInfo$PickerList$undefined ? $OrderInfo$PickerList$lists[$OrderInfo$PickerList$item.picker.rangeBind][$OrderInfo$PickerList$pickedIndex[$OrderInfo$PickerList$item.picker.valueBind]].label || $OrderInfo$PickerList$lists[$OrderInfo$PickerList$item.picker.rangeBind][$OrderInfo$PickerList$pickedIndex[$OrderInfo$PickerList$item.picker.valueBind]] : '请选择' }}</text>
              <view class="arrow-rt"/>
            </view>
          </picker>
          <view  wx:else class="item-value">
            <text>{{ $OrderInfo$PickerList$pickeds[$OrderInfo$PickerList$item.picker.valueBind] !== $OrderInfo$PickerList$undefined ? $OrderInfo$PickerList$lists[$OrderInfo$PickerList$item.picker.rangeBind][$OrderInfo$PickerList$pickedIndex[$OrderInfo$PickerList$item.picker.valueBind]].label || $OrderInfo$PickerList$lists[$OrderInfo$PickerList$item.picker.rangeBind][$OrderInfo$PickerList$pickedIndex[$OrderInfo$PickerList$item.picker.valueBind]] : '未选择' }}</text>
          </view>
        </view>
      </block>
    </view>
  </view>

    <view class="submit-btn-wrap flex--hcenter">
      <button class="submit-btn primary-btn" bindtap="submit">
        确认提交
      </button>
    </view>
  </view>

    <view class="success-dialog {{ successDialogVisible ? '' : 'hidden' }}">
      <view class="success-dialog__mask">
      </view>
      <view class="success-dialog__inner">
        <image src="../assets/images/success-order.png" style="width: 100%; height: 260rpx;"/>
        <view style="margin-top: 98rpx;">
          <image src="../assets/images/success-order-tip.png" style="width: 347rpx; height: 30rpx; margin: auto;"/>
        </view>
        <view class="btn-wrap flex--center">
          <button class="to-vip-btn flex--center">前往VIP用户管理</button>
        </view>
      </view>
      <view class="success-dialog__quit" bindtap="closeDialog">
        <icon type="clear" size="37" color="rgb(225, 225, 225)" style="background: #2E2E2E; border-radius: 50%;"/>
      </view>
    </view>
  </view>